# Local Setup


### Caddy
* Follow the commands in `scripts/init/crts.sh`
* This will add the Caddy root.crt to keychain
  * Required so it will resolve SSL in your local environment
  * Root certs must already exist in the database it to work
