## TODO


### Admin

* Clean up Endpoints components
  * Refactor to move different endpoint type logic into own files
  * For Agent type, the agent select should allow selecting from existing agents
    * Currently show a text input
* Update Agent Drawer to user @tdsk/components


TODO: 
* update other list pages to use PageLayout
* orgId header not working properly
  * a better solution it to move it to the url
    * /org/org-id/projects
    * remove it as a header and query param
