/**
 * TODO: implement code to build WASM Dash
 * The `emcc` executable does not exist in the environment by default
 * The `emsdk` git repo is installed at `./node_modules/emsdk`
 * Need to figure out how to add use it when building Dash
 * Figure out how to run `source node_modules/emsdk/emsdk_env.sh` to add it
 */
