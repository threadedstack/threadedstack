import { aiStream } from '@TBE/endpoints/ai'
import { proxy } from '@TBE/endpoints/proxy'
import { accounts } from '@TBE/endpoints/accounts'

export const endpoints = {
  proxy,
  accounts,
  aiStream,
}
